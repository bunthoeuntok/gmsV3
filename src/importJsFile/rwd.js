export const rwd = {
    state: {
        width: 0,
        height: 0
    },
    mutations: {
        setWidth(state, width) {
            state.width = width
        },
        setHeight(state, height) {
            state.height = height
        }
    }
}